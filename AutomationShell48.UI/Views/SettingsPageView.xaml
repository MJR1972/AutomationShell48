<UserControl x:Class="AutomationShell48.UI.Views.SettingsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <StackPanel>
            <TextBlock Text="Settings" Style="{StaticResource H1TextStyle}" />
            <TextBlock Margin="0,6,0,18"
                       Style="{StaticResource BodyTextStyle}"
                       Text="Application preferences are persisted per user using built-in Settings." />

            <Border Style="{StaticResource CardSurfaceStyle}">
                <StackPanel>
                    <TextBlock Text="Application State" Style="{StaticResource H3TextStyle}" />
                    <TextBlock Margin="0,10,0,0" Text="{Binding Description}" Style="{StaticResource BodyTextStyle}" TextWrapping="Wrap" />
                    <TextBlock Margin="0,12,0,0" Style="{StaticResource CaptionTextStyle}" Text="{Binding CurrentTheme, StringFormat=Current Theme: {0}}" />
                </StackPanel>
            </Border>
        </StackPanel>

        <Border Background="#66000000" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="240" Height="10" />
                <TextBlock Margin="0,10,0,0" Text="{Binding BusyMessage}" Foreground="White" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
